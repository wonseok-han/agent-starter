{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "rm -f package-lock.json && npm install --no-audit --no-fund",
  "buildCommand": "npm run build && node build/prerender.mjs",
  "outputDirectory": "out"
}
